# KMeans-Association-Rule

This code demostrate my knowledge on KMeans and Association rule algorithm.

First clustering is done on the dataset using K-means clustering algorithm. A-priori algorithm is then used to find the frequent item sets.
